interface:
  display_name: "Android Firmware Lab"
  short_description: "Inspect, build, boot, and debug Android firmware"
  default_prompt: "Use $android-firmware-lab to inventory this Android device, validate its artifacts, and plan an evidence-producing boot test."
